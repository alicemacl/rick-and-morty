import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  text: {
    fontSize: 18,
  },
  dot: {
    borderRadius: 50,
    width: 10,
    height: 10,
    marginLeft: 10,
  },
  textContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignItems: 'center',
    paddingBottom: 10,
  },
})
